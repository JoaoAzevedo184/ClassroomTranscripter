"""Cliente HTTP da Udemy com bypass de Cloudflare.

MIGRAÇÃO (Fase 3):
-----------------
Copiar o conteúdo integral de `udemy_transcripter/client.py` pra cá.
Não precisa reescrever — só mudar imports:
    from udemy_transcripter.exceptions import ...
    →
    from classroom_transcripter.core.exceptions import ...

Este é o módulo mais complexo do projeto atual (curl-cffi, Cloudflare bypass),
e ele continua igual — só muda de lugar.
"""
# TODO Fase 3: migrar udemy_transcripter/client.py inteiro
